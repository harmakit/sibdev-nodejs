<!DOCTYPE html>
<html>
  <head>
    <title>Author</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
  </head>
  <body class="bg">

    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="/">Blog</a>
        </div>
        <ul class="nav navbar-nav">
          <li><a href="/">Home</a></li>
          <li <%if(my){%>class="active"<%}%>><a href="/author">Profile</a></li>
          <li><a href="/post/new">Create post</a></li>
          <li><a href="/logout">Logout</a></li>
        </ul>
      </div>
    </nav>


        <div class="container-fluid text-center">
            <h2><%=user.username%></h2>
            <p class="text-muted"><%=user.email%></p>
            <hr>
            <% if (messages[0]) {%>
              <div class="alert alert-info">
                <%=messages%>
              </div>
            <%}%>
        </div>

        <% if (posts) {%>
          <% for (var i = 0; i < posts.length; i++) {%>
            <div class="container-fluid text-center">
              <div class="card">
                <div class="card-body">
                  <h2 class="card-title"><%=posts[i].title%></h2>
                  <p class="card-subtitle"><%=posts[i].description%></p>
                  <a href="/post/<%=posts[i].id%>" class="btn btn-primary">Read More</a>
                </div>
                <div class="text-muted">
                  Posted on <%=posts[0].createdAt%>
                </div>
              </div>
            </div>
          </br>
          <%}%>
        <%}%>

  </body>
</html>
